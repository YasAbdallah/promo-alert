export type Product = {
    id: number,
    title: string,
    slug: string,
    description: string,
    brand: string,
    rating: number,
    price: number,
    images: string[],
    category: string,
}